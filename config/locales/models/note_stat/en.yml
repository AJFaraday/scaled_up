en:
  activerecord:
    models:
      note_stat:
        one: Note Stat
        other: Note Stats
    attributes:
      note_stat:
        name: Note Name
        midi_note: MIDI Note
        cnt: Count